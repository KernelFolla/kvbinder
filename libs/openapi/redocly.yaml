extends:
  - recommended
apis:
  core@v1:
    root: ./openapi/openapi.yaml
    rules:
      no-ambiguous-paths: error
theme:
  openapi:
    schemaExpansionLevel: 2
    generateCodeSamples:
      languages:
        - lang: curl
        - lang: Python
        - lang: JavaScript
